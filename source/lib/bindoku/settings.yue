global TILE_STATES = {
	"none": "-"
	"off":  "0"
	"on":   "1"
}

global CYCLE_ORDER = {
  "1": "none"
  "2": "off"
  "3": "on"
}

global ORDER_LOOKUP = {
  "none": 1
  "off": 2
  "on": 3
}

-- Maximum allowed adjacent to eachother
-- MAX_ADJACENT = 2

global MAX_ATTEMPTS = 100000

-- Maximium seconds to generate puzzle
global MAX_TIME = 5

return {:TILE_STATES, :CYCLE_ORDER, :ORDER_LOOKUP, :MAX_ATTEMPTS, :MAX_TIME}
